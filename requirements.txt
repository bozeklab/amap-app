# AMAP-APP dependencies — single cross-platform lock (Linux, Windows, macOS),
# verified on Python 3.13.
#
# PyTorch: the CUDA (GPU) build is installed by default on Linux and Windows.
# macOS has no CUDA builds, so the standard PyPI build is used there. On a machine
# without an NVIDIA GPU the CUDA build still runs on the CPU. For a smaller,
# CPU-only install, install torch separately from the CPU index, e.g.:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Linux note: the wheels require a reasonably recent system (glibc >= 2.34, e.g.
# Ubuntu 22.04+ / RHEL 9+); GPU use also needs a current NVIDIA driver.
#
# For a Python version other than 3.13, use requirements-unlocked.txt instead.
--extra-index-url https://download.pytorch.org/whl/cu130

# Core dependencies (all platforms)
filelock==3.29.4
fsspec==2026.6.0
imageio==2.37.3
jinja2==3.1.6
lazy-loader==0.5
markupsafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.0
# OpenCV: headless on Linux/macOS (avoids a Qt-plugin clash with PySide6);
# the full build on Windows, matching the previously working setup.
opencv-python-headless==4.13.0.92 ; sys_platform != 'win32'
opencv-python==4.13.0.92 ; sys_platform == 'win32'
packaging==26.2
pandas==2.3.3
pillow==12.2.0
psutil==7.2.2
pyside6==6.11.1
pyside6-addons==6.11.1
pyside6-essentials==6.11.1
python-dateutil==2.9.0.post0
pytz==2026.2
scikit-image==0.26.0
scipy==1.18.0
setuptools==81.0.0
shiboken6==6.11.1
six==1.17.0
sympy==1.14.0
tifffile==2026.6.1
typing-extensions==4.15.0
tzdata==2026.2

# PyTorch — CUDA (GPU) build on Linux/Windows, PyPI build on macOS
torch==2.12.1+cu130 ; sys_platform != 'darwin'
torch==2.11.0 ; sys_platform == 'darwin'

# CUDA (GPU) runtime — pulled in by torch on Linux only
cuda-bindings==13.3.1 ; sys_platform == 'linux'
cuda-pathfinder==1.5.6 ; sys_platform == 'linux'
cuda-toolkit==13.0.2 ; sys_platform == 'linux'
nvidia-cublas==13.1.1.3 ; sys_platform == 'linux'
nvidia-cuda-cupti==13.0.85 ; sys_platform == 'linux'
nvidia-cuda-nvrtc==13.0.88 ; sys_platform == 'linux'
nvidia-cuda-runtime==13.0.96 ; sys_platform == 'linux'
nvidia-cudnn-cu13==9.20.0.48 ; sys_platform == 'linux'
nvidia-cufft==12.0.0.61 ; sys_platform == 'linux'
nvidia-cufile==1.15.1.6 ; sys_platform == 'linux'
nvidia-curand==10.4.0.35 ; sys_platform == 'linux'
nvidia-cusolver==12.0.4.66 ; sys_platform == 'linux'
nvidia-cusparse==12.6.3.3 ; sys_platform == 'linux'
nvidia-cusparselt-cu13==0.8.1 ; sys_platform == 'linux'
nvidia-nccl-cu13==2.29.7 ; sys_platform == 'linux'
nvidia-nvjitlink==13.0.88 ; sys_platform == 'linux'
nvidia-nvshmem-cu13==3.4.5 ; sys_platform == 'linux'
nvidia-nvtx==13.0.85 ; sys_platform == 'linux'
triton==3.7.1 ; sys_platform == 'linux'
